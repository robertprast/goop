services:
  goop:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: goop
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file:
      - .env
    # Distroless has no shell; rely on k8s/compose-external probing of /healthz.
